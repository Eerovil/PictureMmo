CREATE TABLE users (
    name TEXT PRIMARY KEY,
    posx INT  DEFAULT 0,
    posy INT DEFAULT 0,
    red INT DEFAULT 0,
    green INT DEFAULT 0,
    blue INT DEFAULT 0,
    reg0 INT DEFAULT 0,
    reg1 INT DEFAULT 0,
    code TEXT DEFAULT ""
)
